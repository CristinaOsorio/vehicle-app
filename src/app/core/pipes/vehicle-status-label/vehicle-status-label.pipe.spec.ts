import { VehicleStatusLabelPipe } from './vehicle-status-label.pipe';

describe('VehicleStatusLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleStatusLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
